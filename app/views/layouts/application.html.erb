<!DOCTYPE html>
<html>
  <head>
    <title>Ballpark</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
          rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header ckass="navbar fixed-top navbar-light gb-light navbar-expand-lg">
      <div class="container">
        <nav>
          <ul class="nav navbar-nav navbar-right">
            <div class='btn-toolbar' role="toolbar">
              <% if logged_in? %>
                <li>
                  <%= link_to logout_path, data: { "turbo-method": :delete } do %>
                    <button type="button" class="btn btn-primary">ログアウト</button>
                  <% end %>
                </li>
                
                <li>
                  <%= link_to new_video_path do %>
                    <button type="button" class="btn btn-success">投稿</button>
                  <% end %>
                </li>
                <li>
                  <%= link_to places_path do %>
                    <button type="button" class="btn btn-success">球場</button>
                  <% end %>
                </li>
              <% end%>
            </div>
          </ul>
        </nav>
      </div>
    </header>
    <div class="container my-5">
      <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
